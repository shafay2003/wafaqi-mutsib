// This file is intentionally left blank to resolve a build error.
// The project was configured to use MongoDB, but no MONGODB_URI was provided.
// Since the database is not used, this code has been removed.
export default Promise.resolve({});
